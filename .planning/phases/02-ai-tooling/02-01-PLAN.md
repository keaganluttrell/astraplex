---
phase: 02-ai-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - config/config.exs
  - lib/astraplex/system/system.ex
  - lib/astraplex/system/health.ex
  - lib/astraplex_web/endpoint.ex
  - .mcp.json
  - test/astraplex/system/health_test.exs
autonomous: false
requirements: [AI-01, AI-02]

must_haves:
  truths:
    - "System domain exists with a Health resource that returns status, version, uptime, and node"
    - "MCP server is accessible at /ash_ai/mcp in dev environment only"
    - "Claude Code can connect to the MCP server and discover available tools"
    - ".mcp.json is committed so any developer clone gets MCP ready"
  artifacts:
    - path: "lib/astraplex/system/system.ex"
      provides: "System Ash domain with AshAi extension and tools block"
      contains: "use Ash.Domain, extensions: [AshAi]"
    - path: "lib/astraplex/system/health.ex"
      provides: "Health resource with embedded data layer and check action"
      contains: "data_layer: :embedded"
    - path: "lib/astraplex_web/endpoint.ex"
      provides: "AshAi.Mcp.Dev plug in code_reloading block"
      contains: "AshAi.Mcp.Dev"
    - path: ".mcp.json"
      provides: "Claude Code MCP server config with HTTP transport"
      contains: "ash_ai/mcp"
    - path: "test/astraplex/system/health_test.exs"
      provides: "Integration test for Health resource check action"
  key_links:
    - from: "lib/astraplex/system/system.ex"
      to: "lib/astraplex/system/health.ex"
      via: "resources block registration"
      pattern: "resource Astraplex\\.System\\.Health"
    - from: "config/config.exs"
      to: "lib/astraplex/system/system.ex"
      via: "ash_domains config"
      pattern: "ash_domains:.*Astraplex\\.System"
    - from: "lib/astraplex_web/endpoint.ex"
      to: "lib/astraplex/system/system.ex"
      via: "AshAi.Mcp.Dev auto-discovery via otp_app"
      pattern: "otp_app: :astraplex"
    - from: ".mcp.json"
      to: "lib/astraplex_web/endpoint.ex"
      via: "HTTP URL pointing to MCP dev endpoint"
      pattern: "localhost:4000/ash_ai/mcp"
---

<objective>
Add the ash_ai dependency, create a System/Health domain to validate the MCP pipeline, wire the MCP dev server into the Phoenix endpoint, and configure .mcp.json for Claude Code.

Purpose: AI agents (Claude Code) can interact with Ash domains during development via MCP, starting with the System/Health domain and auto-discovering all future domains.
Output: Working MCP server accessible at http://localhost:4000/ash_ai/mcp, System domain with Health resource, integration test, .mcp.json committed.
</objective>

<execution_context>
@/Users/keagan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keagan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ai-tooling/02-CONTEXT.md
@.planning/phases/02-ai-tooling/02-RESEARCH.md

<interfaces>
<!-- Current endpoint.ex code_reloading block (lines 36-41) where MCP plug will be inserted -->
From lib/astraplex_web/endpoint.ex:
```elixir
if code_reloading? do
  socket "/phoenix/live_reload/socket", Phoenix.LiveReloader.Socket
  plug Phoenix.LiveReloader
  plug Phoenix.CodeReloader
  plug Phoenix.Ecto.CheckRepoStatus, otp_app: :astraplex
end
```

From config/config.exs:
```elixir
config :astraplex, ash_domains: []
```

From mix.exs (Ash deps section):
```elixir
# Ash ecosystem
{:ash, "~> 3.0"},
{:ash_postgres, "~> 2.0"},
{:ash_phoenix, "~> 2.0"},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ash_ai dep and create System/Health domain</name>
  <files>mix.exs, mix.lock, config/config.exs, lib/astraplex/system/system.ex, lib/astraplex/system/health.ex, test/astraplex/system/health_test.exs</files>
  <behavior>
    - Test: Calling the Health :check action returns a struct with status :healthy, a non-empty version string, a non-negative uptime_seconds integer, and a non-nil node string
    - Test: The Health resource module is defined and has the expected attributes (status, version, uptime_seconds, node)
  </behavior>
  <action>
    1. Add `{:ash_ai, "~> 0.5"}` to the Ash ecosystem section in mix.exs deps, then run `mix deps.get`.

    2. Create `lib/astraplex/system/system.ex` -- the System Ash domain:
       - `use Ash.Domain, extensions: [AshAi]`
       - `resources` block registering `Astraplex.System.Health`
       - `tools` block with `tool :check_health, Astraplex.System.Health, :check`
       - @moduledoc describing it as the system domain for health checks and operational diagnostics

    3. Create `lib/astraplex/system/health.ex` -- the Health Ash resource:
       - `use Ash.Resource, domain: Astraplex.System, data_layer: :embedded`
       - Attributes (all `public? true`, `allow_nil? false`):
         - `status` -- `:atom`, constraints `one_of: [:healthy, :degraded, :unhealthy]`
         - `version` -- `:string`
         - `uptime_seconds` -- `:integer`
         - `node` -- `:string` (allow_nil? true is fine for this one)
       - Generic action `:check` returning `{:struct, constraints: [instance_of: __MODULE__]}`:
         - Uses `run fn` to compute health data
         - Gets uptime via `:erlang.statistics(:wall_clock)`
         - Gets version via `Application.spec(:astraplex, :vsn) |> to_string()`
         - Gets node via `Node.self() |> to_string()`
         - Returns `{:ok, %__MODULE__{...}}`
         - Has `description "Returns current system health status"`
       - Policy: `policy always() do authorize_if always() end` (open access for dev tooling)
       - @moduledoc describing it as the health check resource

    4. Update `config/config.exs` -- change `ash_domains: []` to `ash_domains: [Astraplex.System]`

    5. Create `test/astraplex/system/health_test.exs`:
       - `use Astraplex.DataCase` (or ExUnit.Case if DataCase requires Ecto setup -- Health is embedded so no DB needed)
       - Test that `Astraplex.System.Health.check!()` (or `Ash.run_action!(Astraplex.System.Health, :check)`) returns a struct with the expected fields
       - Assert status is :healthy
       - Assert version is a non-empty string
       - Assert uptime_seconds is a non-negative integer
       - Assert node is a string

    Note: The exact Ash API for invoking a generic action on an embedded resource may need adjustment. If `Ash.run_action!` is the correct API, use that. Check ash_ai and Ash docs for the right invocation pattern for generic actions. The key is: call the :check action and verify it returns valid health data.
  </action>
  <verify>
    <automated>cd /Users/keagan/repos/astraplex && mix test test/astraplex/system/health_test.exs -x</automated>
  </verify>
  <done>System domain and Health resource compile, the :check action returns valid health data, and the integration test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MCP dev server and create .mcp.json</name>
  <files>lib/astraplex_web/endpoint.ex, .mcp.json</files>
  <action>
    1. Edit `lib/astraplex_web/endpoint.ex` -- add the AshAi.Mcp.Dev plug as the FIRST item inside the `if code_reloading? do` block (before the LiveReloader socket):
       ```elixir
       if code_reloading? do
         plug AshAi.Mcp.Dev,
           protocol_version_statement: "2024-11-05",
           otp_app: :astraplex

         socket "/phoenix/live_reload/socket", Phoenix.LiveReloader.Socket
         plug Phoenix.LiveReloader
         plug Phoenix.CodeReloader
         plug Phoenix.Ecto.CheckRepoStatus, otp_app: :astraplex
       end
       ```
       Use protocol_version_statement "2024-11-05" for maximum client compatibility (per research -- ash_ai v0.5 implements 2025-03-26 but many clients expect the older version).

       Note: The research says the default path is `/ash_ai/mcp`. If `AshAi.Mcp.Dev` accepts a `path` option, do NOT set it explicitly -- let it use the default. Only set it if the default is different from `/ash_ai/mcp`.

    2. Create `.mcp.json` at the project root:
       ```json
       {
         "mcpServers": {
           "astraplex": {
             "type": "http",
             "url": "http://localhost:4000/ash_ai/mcp"
           }
         }
       }
       ```

    3. Verify the project compiles cleanly: `mix compile --warnings-as-errors`

    Important: If the AshAi.Mcp.Dev plug API differs from what the research documented (e.g., different option names, different default path), adapt accordingly. The research is high-confidence but the exact API should be verified from the installed package docs.
  </action>
  <verify>
    <automated>cd /Users/keagan/repos/astraplex && mix compile --warnings-as-errors && test -f .mcp.json && mix test</automated>
  </verify>
  <done>Phoenix endpoint includes AshAi.Mcp.Dev plug in dev-only code_reloading block, .mcp.json exists at project root with HTTP transport config pointing to localhost:4000/ash_ai/mcp, project compiles with zero warnings, all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MCP server connects from Claude Code</name>
  <files>n/a</files>
  <action>
    Human verifies that the MCP server is functional end-to-end by connecting from Claude Code.
  </action>
  <verify>
    <automated>echo "Manual checkpoint -- requires running Phoenix server and Claude Code MCP connection"</automated>
  </verify>
  <done>Claude Code shows astraplex MCP server as connected and can invoke check_health tool.</done>
  <what-built>MCP server exposing System/Health domain actions as tools, accessible to Claude Code via HTTP transport at http://localhost:4000/ash_ai/mcp</what-built>
  <how-to-verify>
    1. Start the Phoenix dev server: `iex -S mix phx.server`
    2. In a separate terminal, verify the MCP endpoint responds: `curl -s http://localhost:4000/ash_ai/mcp` (should return a response, not a 404)
    3. In Claude Code, check that the astraplex MCP server appears in `/mcp` (it reads .mcp.json automatically)
    4. If the MCP server shows as connected, try invoking the check_health tool to verify it returns health data (status, version, uptime, node)
    5. If the MCP server shows as disconnected, verify the Phoenix server is running and the URL is correct
  </how-to-verify>
  <resume-signal>Type "approved" if Claude Code can connect and invoke the health check tool, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` passes (zero warnings)
- `mix test` passes (all tests including new health_test.exs)
- `mix credo --strict` passes
- `.mcp.json` exists at project root with correct HTTP transport config
- System domain registered in config/config.exs ash_domains
- AshAi.Mcp.Dev plug only present in code_reloading? block (dev-only)
- Claude Code can connect to MCP server and call check_health tool
</verification>

<success_criteria>
- AI agent (Claude Code) connects to MCP server at localhost:4000/ash_ai/mcp
- check_health tool returns system health data (status, version, uptime, node)
- New domains added in future phases with AshAi extension will be auto-discovered (verified by otp_app config)
- .mcp.json committed to repo for immediate developer use
</success_criteria>

<output>
After completion, create `.planning/phases/02-ai-tooling/02-01-SUMMARY.md`
</output>
