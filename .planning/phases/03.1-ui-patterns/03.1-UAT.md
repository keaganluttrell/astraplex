---
status: diagnosed
phase: 03.1-ui-patterns
source: [03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md, 03.1-03-SUMMARY.md]
started: 2026-03-10T17:00:00Z
updated: 2026-03-10T17:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Avatar Initials Centered
expected: Log in as any user. Look at the sidebar user area at the bottom. Your initials should be perfectly centered (both horizontally and vertically) inside the colored circle. No offset or misalignment.
result: issue
reported: "still is not centered in the circle"
severity: cosmetic

### 2. Breadcrumb Navigation on Dashboard
expected: Navigate to the dashboard (Home). The top bar should show a breadcrumb trail (e.g., "Home") instead of a page_header below the top bar. Breadcrumbs use slash separators.
result: pass

### 3. Breadcrumb Navigation on Admin User List
expected: As admin, navigate to Admin > Users. The top bar should show a breadcrumb trail like "Admin / Users" with the earlier segments being clickable links.
result: pass

### 4. No page_header in Content Area
expected: On both Dashboard and Admin User List pages, there should be NO separate page_header component below the top bar. The breadcrumbs in the top bar replace it entirely.
result: pass

### 5. User List Sort Stability
expected: As admin on /admin/users, change a user's role (e.g., promote staff to admin). After the role change, the user list should NOT re-sort or jump — users remain in alphabetical order by email.
result: pass

### 6. Mobile Dock Still Works
expected: Resize browser to mobile width. The bottom dock with Home, Inbox, Apps, Create buttons should still appear. Tap Apps — the bottom sheet should slide up with navigation links.
result: pass

### 7. Top Bar Fallback Without Breadcrumbs
expected: If a page doesn't pass breadcrumb_path, the top bar should fall back to showing an "Astraplex" link (not break or show empty).
result: pass

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0

## Gaps

- truth: "User dropdown shows avatar with centered initials in circle"
  status: resolved
  reason: "User reported: still is not centered in the circle"
  severity: cosmetic
  test: 1
  root_cause: "daisyUI .avatar > div sets display:block (specificity 0,1,1) which overrides Tailwind flex utility (0,1,0). Class 'avatar placeholder' should be 'avatar avatar-placeholder' — the hyphenated class triggers daisyUI's built-in flex centering on > div."
  artifacts:
    - path: "lib/astraplex_web/components/ui.ex"
      issue: "Line 77: uses 'avatar placeholder' instead of 'avatar avatar-placeholder'"
  missing:
    - "Change outer div class from 'avatar placeholder' to 'avatar avatar-placeholder'"
  debug_session: ""
