---
phase: 01-engineering-quality
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .credo.exs
  - config/dev.exs
  - CLAUDE.md
autonomous: false
requirements:
  - QUAL-03
  - QUAL-04
  - QUAL-05
  - QUAL-07

must_haves:
  truths:
    - "Running `mix credo --strict` analyzes the codebase and passes"
    - "Running `mix dialyzer` type-checks the codebase (after initial PLT build)"
    - "Git pre-commit hook rejects badly formatted code"
    - "Git pre-push hook rejects when tests fail"
    - "CLAUDE.md exists with code conventions, architecture rules, and commit conventions"
  artifacts:
    - path: ".credo.exs"
      provides: "Credo strict configuration"
      contains: "strict: true"
    - path: "config/dev.exs"
      provides: "git_hooks configuration"
      contains: "pre_commit"
    - path: "CLAUDE.md"
      provides: "AI usage rules and code conventions"
      contains: "Conventional Commits"
  key_links:
    - from: "config/dev.exs"
      to: "git_hooks library"
      via: "config :git_hooks"
      pattern: "config :git_hooks"
    - from: ".credo.exs"
      to: "mix credo"
      via: "Credo config loading"
      pattern: "strict: true"
---

<objective>
Configure static analysis (Credo strict + Dialyxir), set up git hooks for quality enforcement, and create CLAUDE.md with project conventions.

Purpose: Static analysis catches bugs and code smells before they reach the test suite (QUAL-03). Git hooks enforce quality at commit/push time with no escape hatch (QUAL-04, QUAL-05). CLAUDE.md encodes the project's architectural rules so Claude follows them in all future phases (QUAL-07).
Output: Credo strict config, Dialyxir ready, git hooks installed, and comprehensive CLAUDE.md.
</objective>

<execution_context>
@/Users/keagan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/keagan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engineering-quality/01-CONTEXT.md
@.planning/phases/01-engineering-quality/01-RESEARCH.md
@.planning/phases/01-engineering-quality/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Credo strict, Dialyxir, and git_hooks</name>
  <files>.credo.exs, config/dev.exs</files>
  <action>
1. Create `.credo.exs` with strict mode configuration. Use the full config from the RESEARCH.md code example:
   - `strict: true`
   - Include all Consistency, Readability, Refactoring, Warning, and Design checks
   - Set `MaxLineLength` to 120
   - `AliasUsage` with `if_nested_deeper_than: 2`
   - Disable `Credo.Check.Readability.Specs` (rely on Dialyzer instead)
   - Include `lib/` and `test/` directories
   - Exclude `_build/`, `deps/`, `node_modules/`

2. Run `mix dialyzer` once to build the initial PLT. This takes 5-15 minutes on first run. The PLT is cached in `_build` and subsequent runs are fast (seconds). Do NOT skip this step -- pre-push hooks need it ready.

3. Configure git_hooks in `config/dev.exs`. Add at the end of the file (or within an existing `if Mix.env() == :dev` block if one exists):

```elixir
if Mix.env() == :dev do
  config :git_hooks,
    auto_install: true,
    verbose: true,
    hooks: [
      pre_commit: [
        tasks: [
          {:cmd, "mix format --check-formatted"},
          {:cmd, "mix compile --warnings-as-errors"},
          {:cmd, "mix credo --strict"}
        ]
      ],
      pre_push: [
        tasks: [
          {:cmd, "mix test --color"},
          {:cmd, "mix dialyzer"}
        ]
      ]
    ]
end
```

Per research recommendation: Dialyzer runs on pre-push only (not pre-commit) to keep commits fast. Credo is fast enough for pre-commit. E2E tests are CI-only per user decision -- NOT in hooks.

4. Run `mix deps.compile git_hooks` to trigger auto-install of the hooks.

5. Verify hooks are installed:
```bash
ls -la .git/hooks/pre-commit .git/hooks/pre-push
```

6. Verify static analysis passes on the current codebase:
```bash
mix credo --strict
mix dialyzer
```

Note: If Credo or Dialyzer report issues with generated Phoenix code, fix them. The codebase must be clean from the start -- zero tolerance per user decision.
  </action>
  <verify>
    <automated>cd /Users/keagan/repos/astraplex && mix credo --strict && mix format --check-formatted && echo "PASS: Static analysis clean"</automated>
  </verify>
  <done>Credo strict passes with zero issues, Dialyzer PLT built, git hooks installed (pre-commit: format+compile+credo, pre-push: test+dialyzer)</done>
</task>

<task type="auto">
  <name>Task 2: Create CLAUDE.md with project conventions and architecture rules</name>
  <files>CLAUDE.md</files>
  <action>
Create `CLAUDE.md` at the project root with all conventions from CONTEXT.md. This file is read by Claude at the start of every session and encodes strict rules (not guidelines).

The file MUST contain these sections:

**1. Project Overview**
- Astraplex: real-time internal messaging platform
- Stack: Elixir, Phoenix 1.8, Ash Framework 3.x, LiveView, PostgreSQL
- Design: daisyUI 5 with DaisyUIComponents, corporate theme

**2. Commit Conventions**
- Conventional Commits format: `type(scope): description`
- Types: feat, fix, refactor, test, docs, chore, style, perf, ci
- Scopes match Ash domains: accounts, messaging, notifications, presence, admin
- Plus infrastructure scopes: infra, deps, docs, test, ui
- Examples: `feat(messaging): add message threading`, `fix(auth): handle expired session redirect`

**3. Architecture Rules (STRICT -- violations are bugs)**
- NEVER use raw Ecto queries -- ALL data access through Ash actions
- Domains MUST NOT reach into other domains' internals
- Resources live in their domain directory (e.g., `lib/astraplex/accounts/user.ex`)
- Policies on EVERY Ash action -- no exceptions
- Integration test for every Ash action
- No business logic in LiveView modules

**4. File Conventions**
- Module structure order: @moduledoc, use/import/alias, @attributes, Ash resource DSL blocks, public functions, private functions
- No modules over ~300 lines -- split if approaching
- Group related functions together
- @doc on all public functions
- @spec on public API functions (functions called from outside the module)

**5. LiveView Rules**
- Target: <150 lines per LiveView module
- LiveView handles: mount/handle_params, handle_event (delegates to Ash), render (composes components)
- LiveView does NOT: query database directly, contain business rules, do complex data transforms
- Function components preferred, use slots for flexibility
- All data fetching through Ash actions, never Repo calls

**6. Testing Rules**
- Integration tests for every Ash action (covers policies, validations, calculations)
- No unit tests (integration and E2E provide more value for domain-driven Ash architecture)
- Use Smokestack factories for prerequisite/bulk data setup
- Use Ash actions directly for the behavior being tested
- NEVER use raw Ecto (`Repo.insert!`) in tests -- always Smokestack or Ash actions
- NEVER use ExMachina
- E2E tests tagged with `@moduletag :e2e`, excluded by default, CI runs with `--include e2e`

**7. Design System**
- Use DaisyUIComponents for all UI components
- Corporate daisyUI theme (`data-theme="corporate"`)
- Tailwind CSS 4 utility classes for layout/spacing
- No custom CSS unless absolutely necessary

**8. Development Commands**
- `mix test` -- run integration tests (excludes E2E)
- `mix test --include e2e` -- run all tests including E2E
- `mix credo --strict` -- static analysis
- `mix dialyzer` -- type checking
- `mix format` -- format code
- `mix format --check-formatted` -- check formatting

**9. Domain Structure**
```
lib/astraplex/           # Ash domains and resources
  accounts/              # User management, auth
  messaging/             # Messages, channels, conversations
  notifications/         # Notification delivery
lib/astraplex_web/       # Web layer
  components/            # Design system, function components
  live/                  # LiveView modules
  layouts/               # Layout templates
```

Write the file in a clear, scannable format. Use headers, bullet points, and code blocks. Every rule should be unambiguous -- Claude must be able to follow it without interpretation.
  </action>
  <verify>
    <automated>cd /Users/keagan/repos/astraplex && test -f CLAUDE.md && grep -c "NEVER" CLAUDE.md | xargs test 3 -le && echo "PASS: CLAUDE.md exists with strict rules"</automated>
  </verify>
  <done>CLAUDE.md exists at project root with code conventions, architecture rules, commit conventions, testing rules, and domain structure that Claude will follow in all future phases</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify git hooks and CLAUDE.md</name>
  <files>CLAUDE.md, .git/hooks/pre-commit, .git/hooks/pre-push</files>
  <action>
Human verifies that git hooks reject bad code and CLAUDE.md content matches expectations.

What was built: Static analysis (Credo strict + Dialyxir), git hooks (pre-commit: format+compile+credo, pre-push: test+dialyzer), and CLAUDE.md with all project conventions.
  </action>
  <verify>
Steps to verify:

1. Test pre-commit hook -- create a badly formatted file and try to commit:
   ```bash
   echo "defmodule   Bad do end" > /tmp/bad.ex
   cp /tmp/bad.ex lib/bad.ex
   git add lib/bad.ex
   git commit -m "test: bad format"
   # Should be REJECTED by pre-commit hook (format check fails)
   rm lib/bad.ex
   git checkout -- . 2>/dev/null; git reset HEAD . 2>/dev/null
   ```

2. Review CLAUDE.md: Open the file and verify it matches your expectations for conventions, architecture rules, and commit format. This file governs all future Claude interactions with this project.

3. Verify `mix credo --strict` passes (should show no issues).
  </verify>
  <done>User confirms pre-commit hook rejects bad code and CLAUDE.md content is approved. Type "approved" or describe any issues.</done>
</task>

</tasks>

<verification>
- `mix credo --strict` passes with zero issues
- `mix dialyzer` completes successfully (PLT built)
- `mix format --check-formatted` passes
- `.git/hooks/pre-commit` exists and is executable
- `.git/hooks/pre-push` exists and is executable
- `CLAUDE.md` exists and contains all required sections
- Pre-commit hook rejects badly formatted code
</verification>

<success_criteria>
- Static analysis tools configured and passing on the full codebase
- Git pre-commit hook blocks on format, compile warnings, and Credo issues
- Git pre-push hook blocks on test failures and Dialyzer errors
- No bypass mechanism -- hooks always enforced
- CLAUDE.md comprehensively documents conventions, architecture rules, commit format, testing rules, and domain structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-engineering-quality/01-03-SUMMARY.md`
</output>
